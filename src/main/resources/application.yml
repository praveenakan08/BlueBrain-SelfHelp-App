server:
  port: 8080

spring:
  application:
    name: bluebrain-backend
  datasource:
    url: jdbc:postgresql://localhost:5432/bluebrain_db
    username: your_username
    password: your_password
#    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
      database: postgresql
      database-platform: org.hibernate.dialect.PostgreSQLDialect

application:
  security:
    jwt:
      secret-key: d4d130001ed8e0b688eb1bc398a7dbf8
      expiration: 3600
      refresh-token:
          expiration: 3600
